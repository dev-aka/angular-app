import { MenuSelectedDirective } from './menu-selected.directive';

describe('MenuSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
